{
  "name": "libstate-node",
  "version": "1.0.0",
  "description": "Reference statistics tracking tool build on NodeJS and React",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"pnpm run dev:api\" \"pnpm run dev:client\"",
    "dev:api": "pnpm --filter api dev",
    "dev:client": "pnpm --filter client dev",
    "db:migrate": "pnpm --filter api migrate",
    "db:seed": "pnpm --filter api seed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
